from distutils.core import setup

setup( name = "tcpserver",
        version = "1.0.0",
        description = " Python 3.x based TCP SERVER",
        author = "Ayush",
        scripts = ["bin/tcpserver"],
        packages = ["src"])
